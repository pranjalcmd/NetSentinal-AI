# Scripts

Use this folder for setup/check helpers once the team wires real nDPI execution and authorized sample PCAPs.

Suggested future scripts:

- `check_ndpi.sh` / `.ps1` — verify nDPI build
- `run_pcap_demo.sh` — run a deterministic PCAP through the adapter
- `seed_demo.py` — rebuild JSON fixtures
